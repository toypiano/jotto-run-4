describe("'resetGame' action dispatcher", () => {
  test("updates 'secretWord' state correctly after new word button is clicked", () => {});
  test("clears 'guessedWords' state after new word button is clicked", () => {});
  test("'success' state is false after new word button is clicked", () => {});
});
